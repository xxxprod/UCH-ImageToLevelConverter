<Window x:Class="UCH_ImageToLevelConverter.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UCH_ImageToLevelConverter.Views"
        xmlns:viewModels="clr-namespace:UCH_ImageToLevelConverter.ViewModels"
        xmlns:tools="clr-namespace:UCH_ImageToLevelConverter"
        mc:Ignorable="d"
        Title="Ultimate Chicken Horse Level Designer" Height="750" Width="1100"
        Icon="../Icons/UCH_32x32.png">
    <ContentControl Content="{Binding}">
        <ContentControl.ContentTemplateSelector>
            <tools:ContentViewSelector>
                <tools:ContentViewSelector.Templates>
                    <DataTemplate DataType="{x:Type viewModels:ImageSelectorViewModel}">
                        <local:ImageSelectorView />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewModels:LevelEditorViewModel}">
                        <local:LevelEditorView />
                    </DataTemplate>
                </tools:ContentViewSelector.Templates>
            </tools:ContentViewSelector>
        </ContentControl.ContentTemplateSelector>
    </ContentControl>
</Window>
